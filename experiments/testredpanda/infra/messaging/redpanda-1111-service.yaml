apiVersion: v1
kind: Service
metadata:
  name: redpanda-1111
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: redpanda-1111
  ports:
    - port: 9092
      targetPort: 9092
      protocol: TCP
