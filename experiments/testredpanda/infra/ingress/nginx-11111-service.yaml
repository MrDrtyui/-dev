apiVersion: v1
kind: Service
metadata:
  name: nginx-11111
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: nginx-11111
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
