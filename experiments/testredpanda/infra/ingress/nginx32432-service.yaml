apiVersion: v1
kind: Service
metadata:
  name: nginx32432
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: nginx32432
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
