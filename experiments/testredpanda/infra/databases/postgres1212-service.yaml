apiVersion: v1
kind: Service
metadata:
  name: postgres1212
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: postgres1212
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
