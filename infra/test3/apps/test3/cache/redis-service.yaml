apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: test3
  labels:
    app: redis
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
