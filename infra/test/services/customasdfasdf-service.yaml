apiVersion: v1
kind: Service
metadata:
  name: customasdfasdf
  namespace: default
  labels:
    app: customasdfasdf
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: customasdfasdf
  ports:
    - name: main
      port: 8080
      targetPort: 8080
      protocol: TCP
