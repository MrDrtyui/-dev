apiVersion: v1
kind: Service
metadata:
  name: prometheusasdfasdf
  namespace: default
  labels:
    app: prometheusasdfasdf
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: prometheusasdfasdf
  ports:
    - name: main
      port: 9090
      targetPort: 9090
      protocol: TCP
