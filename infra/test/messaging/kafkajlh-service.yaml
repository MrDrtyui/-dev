apiVersion: v1
kind: Service
metadata:
  name: kafkajlh
  namespace: default
  labels:
    app: kafkajlh
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: kafkajlh
  ports:
    - name: main
      port: 9092
      targetPort: 9092
      protocol: TCP
