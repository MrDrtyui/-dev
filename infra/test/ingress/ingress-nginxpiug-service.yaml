apiVersion: v1
kind: Service
metadata:
  name: ingress-nginxpiug
  namespace: default
  labels:
    app: ingress-nginxpiug
    managed-by: endfield
spec:
  type: LoadBalancer
  selector:
    app: ingress-nginxpiug
  ports:
    - name: main
      port: 80
      targetPort: 80
      protocol: TCP
