apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: default
  labels:
    app: mongodb
    managed-by: endfield
type: Opaque
stringData:
  MONGO_INITDB_ROOT_PASSWORD: "changeme"
