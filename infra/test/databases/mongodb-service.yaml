apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: default
  labels:
    app: mongodb
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: mongodb
  ports:
    - name: main
      port: 27017
      targetPort: 27017
      protocol: TCP
