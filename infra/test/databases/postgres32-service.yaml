apiVersion: v1
kind: Service
metadata:
  name: postgres32
  namespace: default
  labels:
    app: postgres32
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: postgres32
  ports:
    - name: main
      port: 5432
      targetPort: 5432
      protocol: TCP
