apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: default
  labels:
    app: redis
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - name: main
      port: 6379
      targetPort: 6379
      protocol: TCP
