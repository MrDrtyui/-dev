apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: default
  labels:
    app: postgres
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: main
      port: 5432
      targetPort: 5432
      protocol: TCP
