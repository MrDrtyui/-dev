apiVersion: v1
kind: Service
metadata:
  name: custom
  namespace: default
  labels:
    app: custom
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: custom
  ports:
    - name: main
      port: 8080
      targetPort: 8080
      protocol: TCP
