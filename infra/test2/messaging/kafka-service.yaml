apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: default
  labels:
    app: kafka
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: kafka
  ports:
    - name: main
      port: 9092
      targetPort: 9092
      protocol: TCP
