apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: default
  labels:
    app: prometheus
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: prometheus
  ports:
    - name: main
      port: 9090
      targetPort: 9090
      protocol: TCP
