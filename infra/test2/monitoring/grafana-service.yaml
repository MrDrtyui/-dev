apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: default
  labels:
    app: grafana
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: grafana
  ports:
    - name: main
      port: 3000
      targetPort: 3000
      protocol: TCP
