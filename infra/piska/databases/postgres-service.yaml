apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: infra-redis
  labels:
    app: postgres
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: main
      port: 5432
      targetPort: 5432
      protocol: TCP
