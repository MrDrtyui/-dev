kafka:
  namespaceOverride: "infra-kafka"

  replicaCount: 1

  persistence:
    enabled: true
    size: 20Gi

  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
    limits:
      cpu: "2000m"
      memory: "4Gi"

  zookeeper:
    enabled: false

  kraft:
    enabled: true
