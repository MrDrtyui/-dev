# Production overrides — edit this file for prod deployments
kafka:
  replicaCount: 3

  persistence:
    size: 50Gi

  resources:
    requests:
      cpu: "1000m"
      memory: "2Gi"
    limits:
      cpu: "4000m"
      memory: "8Gi"
