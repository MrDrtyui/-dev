apiVersion: v1
kind: Service
metadata:
  name: redpanda
  namespace: test
  labels:
    app: redpanda
    managed-by: endfield
spec:
  type: ClusterIP
  selector:
    app: redpanda
  ports:
    - name: main
      port: 9092
      targetPort: 9092
      protocol: TCP
