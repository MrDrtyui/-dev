cert-manager:
  namespaceOverride: "infra-cert-manager"

  installCRDs: true

  replicaCount: 1

  resources:
    requests:
      cpu: "50m"
      memory: "64Mi"
    limits:
      cpu: "200m"
      memory: "256Mi"
